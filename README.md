# EPIC Hitploq admin tool

## Backend (do this first)
Run backend (auto builds frontend)
```bash
cd backend/
mvn spring-boot:run
```
reachable via localhost:3001

## Frontend dev (optional live reload dev server)
Run dev frontend
```bash
cd frontend/
npm run dev
```
